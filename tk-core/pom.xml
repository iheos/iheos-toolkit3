<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>gov.nist.hit.ds</groupId>
        <artifactId>toolkit3</artifactId>
        <version>3.0.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <groupId>gov.nist.hit.ds</groupId>
    <version>3.0.0-SNAPSHOT</version>
    <artifactId>tk-core</artifactId>
    <packaging>pom</packaging>
    
    <modules>
        <module>../toolkit</module>
        <module>../common-datatypes</module>
        <module>../utilities</module>
        <module>../http</module>
        <module>../soap-support</module>
        <module>../actor-transaction</module>
        <module>../site-management</module>
        <module>../event-log</module>
        <module>../sim-support</module>
        <module>../xdsException</module>
        <module>../httpSoapValidator</module>
        <!-- Leave in core if possible
            These APIs are internal connections - not the external services offered
            Enables modularity within toolkit
        -->
        <module>../tk-apis</module>
        
        <!-- 
            Move out of core 
        -->
        <!-- Make separate -->
        <module>../repository</module>
        <!-- Split into 
             ds-base - Document Sharing core functionality
                        includes schema, validation
             ds-sims - Simulators
        -->
        <module>../ds-sims</module>
    </modules>
    
    <dependencies>
        <dependency>
            <groupId>gov.nist.hit.ds</groupId>
            <artifactId>actor-transaction</artifactId>
        </dependency>
        <dependency>
            <groupId>gov.nist.hit.ds</groupId>
            <artifactId>xdsException</artifactId>
        </dependency>
        <dependency>
            <groupId>gov.nist.hit.ds</groupId>
            <artifactId>httpSoapValidator</artifactId>
        </dependency>
        <dependency>
            <groupId>gov.nist.hit.ds</groupId>
            <artifactId>utilities</artifactId>
        </dependency>
        <dependency>
            <groupId>gov.nist.hit.ds</groupId>
            <artifactId>toolkit</artifactId>
        </dependency>
        <dependency>
            <groupId>gov.nist.hit.ds</groupId>
            <artifactId>http</artifactId>
        </dependency>
        <dependency>
            <groupId>gov.nist.hit.ds</groupId>
            <artifactId>soap-support</artifactId>
        </dependency>
        <dependency>
            <groupId>gov.nist.hit.ds</groupId>
            <artifactId>sim-support</artifactId>
        </dependency>
        <dependency>
            <groupId>gov.nist.hit.ds</groupId>
            <artifactId>site-management</artifactId>
        </dependency>
        <dependency>
            <groupId>gov.nist.hit.ds</groupId>
            <artifactId>event-log</artifactId>
        </dependency>
        <dependency>
            <groupId>gov.nist.hit.ds</groupId>
            <artifactId>repository</artifactId>
        </dependency>
        <dependency>
            <groupId>com.sun.ebxml</groupId>
            <artifactId>ebxmlrr-common</artifactId>
        </dependency>
    </dependencies>
</project>
