package gov.nist.diagnostics

import org.gradle.api.Project
import org.gradle.api.Plugin

class DiagnosticsPlugin implements Plugin<Project> {
    void apply(Project target) {
       // target.task('hello', type: ValidationTask)
    }
}
